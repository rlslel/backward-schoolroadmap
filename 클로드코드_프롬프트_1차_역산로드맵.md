# Claude Code 프롬프트 — 1차 과제 「학교업무 역산 로드맵」

> 사용법: 새 폴더를 만들고 `claude` 실행 후, 아래 `---` 사이 내용을 통째로 붙여넣으세요.
> 또는 이 파일을 프로젝트 루트에 `BRIEF.md`로 저장하고 "BRIEF.md 읽고 시작해줘"라고 하셔도 됩니다.

---

# 프로젝트: 학교업무 역산(D-day) 로드맵 웹앱

## 0. 너의 역할과 이 문서의 지위

너는 이 프로젝트의 유일한 개발자다. 나는 초등학교 교사이고, 세종시교육청 AI 현장 지원단에 제출할
결과물을 만들고 있다.

**교육청이 실제로 제시한 조건은 아래 세 가지뿐이고, 형태는 자유다.**

1. 받아서 바로 쓸 수 있다 — 다른 학교·기관 교직원이 **유료 계정 없이** 그대로 사용 가능
2. 사용법 1쪽이 붙어 있다 — 문제·해결·화면·효과를 담은 1쪽 안내 자료
3. 개인정보 없이 돌아간다 — 가상의 예시 데이터로 시연 가능

서버 금지, 로그인 금지, 네트워크 요청 금지는 **교육청 조건이 아니다.** 이전 기획 단계에서 비용과
개인정보를 확실히 피하려고 스스로 좁혀둔 해석이었다. 아래 「절대 규칙」은 내가 그 세 조건을
지키기 위해 직접 정한 것이며, 그 점을 알고 지켜라.

구현이 어려우면 기능을 빼되, 규칙을 어기지 마라.

작업 진행 방식:
- 각 단계가 끝날 때마다 멈추고 나에게 확인을 받아라. 한 번에 전부 만들지 마라.
- 규칙과 충돌하는 요구가 내게서 나오면, 그냥 따르지 말고 충돌한다는 점을 먼저 알려라.
- 커밋은 단계마다 하나씩, 한글 커밋 메시지로.

## 1. 절대 규칙 (위반 시 결과물 실격)

1. **백엔드 금지.** 서버, 데이터베이스, 서버리스 함수, 인증 서비스를 일절 쓰지 않는다.
   Firebase, Supabase, Vercel Functions 등 전부 금지.
2. **로그인 금지.** 계정, 회원가입, OAuth 없음. 접속하면 바로 쓸 수 있어야 한다.
3. **네트워크 요청은 딱 한 곳만 허용한다 — 사용자가 지정한 구글 시트 읽기.**
   그 외 모든 외부 요청은 금지다. CDN 스크립트, 웹폰트, 애널리틱스, 오류 수집, 광고 전부 금지
   (전부 번들에 포함). 시트는 **읽기만** 한다. 앱이 시트에 쓰는 기능은 만들지 마라.
   구글 로그인, API 키, 앱스스크립트도 쓰지 마라 — 「웹에 게시」로 공개된 CSV 주소만 읽는다.
4. **개인정보 입력란 금지.** 학생·교직원 성명, 연락처, 학번을 담는 필드를 만들지 않는다.
   담당자는 성명이 아니라 **직책**으로만 표기한다 (예: "5학년 부장", "체육 담당").
5. **앱이 만들어내는 데이터는 전부 브라우저 localStorage에만 저장.** 체크 상태, 필터, 개인 메모가
   여기 해당한다. 앱에서 밖으로 **나가는** 요청은 만들지 마라. 시트에서 **들어오는** 읽기 한 건만
   존재한다. 즉 개인정보가 흘러나갈 경로는 여전히 0개다.
6. **UI 전체 한국어.** 초등 교사가 쓰는 용어로. 개발 용어를 화면에 노출하지 마라.
7. **산출물은 두 가지 형태로 배포 가능해야 한다.**
   - GitHub Pages용 정적 빌드 (**기본 배포 방식.** 링크 하나로 접속)
   - `.html` 파일 하나만 받아서 더블클릭하면 오프라인으로 작동하는 단일 파일 빌드
     (업무망 차단 대비용. 인터넷이 없으므로 시트를 못 읽어 **개인 모드로만** 동작한다.
     우선순위가 가장 낮다. 일정이 빠듯하면 이것부터 뺀다.)

## 2. 기술 스택 (이대로 쓸 것)

- Vite + React 18 + TypeScript
- Tailwind CSS (빌드 타임에 컴파일되므로 허용. CDN 방식은 금지)
- 상태관리 라이브러리 없이 React 기본 훅만 사용
- `vite-plugin-singlefile` — 단일 HTML 빌드용
- 날짜 계산은 외부 라이브러리 없이 직접 구현 (`Date` 만 사용)
- PDF는 라이브러리를 쓰지 말고 **`window.print()` + 인쇄 전용 CSS**로 처리
  (jsPDF·html2canvas 금지. 한글 폰트 임베딩 문제 때문이다)

빌드 스크립트:
```
npm run dev      # 개발
npm run build    # GitHub Pages용 정적 빌드 → dist/
npm run build:single   # 단일 HTML 빌드 → dist-single/index.html
```

**GitHub Pages 경로 설정.** `vite.config.ts`의 `base`를 `'/backward-schoolroadmap/'`으로 지정한다.
(배포 주소: `https://rlslel.github.io/backward-schoolroadmap/`)
이 값이 기본값(`'/'`)이면 GitHub Pages에 올렸을 때 화면이 흰 페이지로만 뜬다. 단일 HTML 빌드는
반대로 `base: './'`여야 한다. 두 빌드가 서로 다른 `base`를 쓰도록 설정을 분리하라.

**localStorage를 못 쓰는 경우 대비.** 학교 PC의 브라우저 정책이나 시크릿 모드에서는 저장이
막힐 수 있다. 저장·읽기를 전부 `try/catch`로 감싸고, 실패하면 앱이 죽지 말고 화면 위에
`이 브라우저에서는 저장이 되지 않아 새로고침하면 내용이 사라집니다`라고 알린 뒤 그대로 쓰게 한다.

## 3. 데이터 모델

```ts
export type Category = "legal" | "annual" | "grant" | "custom";
// legal: 법정·필수 공통 / annual: 통상 학사·연례 행사
// grant: 공모·목적 사업   / custom: 학교 자율·특색 사업

export interface SubTask {
  id: string;
  title: string;         // "물품 구입 품의"
  offsetDays: number;    // 앵커 기준 상대일. -30, -14, +3
  draftTitle?: string;   // 기안 제목 예시 (복사 버튼 대상)
  attachments?: string[];// 붙임 서류 목록
  caution?: string;      // 자주 틀리는 점 한 줄
}

export interface Task {
  id: string;
  title: string;
  category: Category;
  dept: string;              // "공통" | "체육" | "연구" | "정보" | "생활" | "방과후" ...
  grades?: number[];         // 관련 학년. 비우면 전 학년
  anchor:
    | { mode: "date"; date: string }              // "2026-05-08"
    | { mode: "week"; month: number; week: number }; // 5월 2주차
  basis?: string;            // 근거 법령·지침명. 예: "학교체육진흥법 시행규칙"
  subtasks: SubTask[];
  enabled: boolean;          // grant 계열은 기본 false
  memo?: string;             // 2차 과제에서 확장할 자리. 지금은 필드만 만들어 둔다
}

export interface Store {
  v: 1;                      // 스키마 버전. 절대 빼지 마라
  schoolYear: number;        // 2026 (학년도. 2026학년도 = 2026. 3. 1. ~ 2027. 2. 말일)

  // 업무 목록 전체를 저장하지 마라. 사용자가 "바꾼 것"만 저장한다. (아래 설명 참조)
  overrides: Record<string, {       // 키는 시드 업무의 id
    anchor?: Task["anchor"];        // 사용자가 고친 앵커
    enabled?: boolean;              // 사용자가 켜고 끈 상태
    memo?: string;
  }>;
  customTasks: Task[];              // 사용자가 직접 추가한 자율·특색 업무만

  sheetUrl?: string;                // 학교 공유 시트의 「웹에 게시」 CSV 주소
  checks: Record<string, boolean>;  // "taskId::subtaskId" → 완료 여부
  ui: {
    dept: string;
    grade: number | "all";     // 학년 필터. 1~6 또는 "all"
    category: Category | "all";
    month: number | "all";
  };
}
```

**스키마 버전(`v`)은 반드시 저장한다.** 2차 과제에서 필드가 늘어날 때 마이그레이션이 필요하다.
localStorage 읽기 함수는 `v`가 없거나 다르면 안전하게 초기화하고, 사용자에게 알린다.

### 3-1. 저장 원칙 — 시드가 원본이고, 저장소에는 "바뀐 것"만 둔다

화면에 그릴 목록은 이렇게 만든다.

```
표시할 업무 = 시드 데이터(tasks.seed.ts)
             + overrides 적용 (사용자가 고친 앵커·켜짐 여부)
             + customTasks (사용자가 직접 추가한 업무)
```

**업무 목록 전체를 localStorage에 통째로 복사해 두면 안 된다.** 그렇게 하면 다음 버전에서 시드에
업무를 추가하거나 근거를 채워 넣어도, 이미 앱을 써 본 사람의 화면에는 옛날 목록이 그대로 남는다.
사용자는 앱을 업데이트했는데 아무것도 안 바뀌는 상태가 된다.

### 3-2. id는 한 번 정하면 영구히 바꾸지 않는다

`Task.id`와 `SubTask.id`는 **불변**이다. 제목이나 날짜를 고치더라도 id는 그대로 둔다.
id를 바꾸면 그 항목에 걸려 있던 사용자의 완료 체크와 앵커 수정이 조용히 사라진다.

읽어 들일 때 시드에 더 이상 존재하지 않는 `checks`·`overrides` 키가 있으면 버린다. 다만 **버린
개수가 1개 이상이면 화면에 한 번 알린다.** 예: `이전 버전에서 완료 표시했던 3개 항목이 목록에서
빠졌습니다.` 조용히 사라지는 것이 가장 나쁘다.

### 3-3. 구글 시트 연동 (학교 전체 일정 공유 수단)

**왜 시트인가.** 여러 교직원이 같은 행사일을 보려면 데이터가 한 군데 있어야 한다. 보통은 서버를
두지만, 서버는 과제가 끝난 뒤에도 누군가 계속 운영해야 한다. 학교가 이미 쓰고 있는 구글 시트에
그 역할을 맡기면 운영 주체가 학교 자신이 되고, 비용도 0원이며, 앱이 사라져도 데이터는 남는다.

**역할 분담을 지켜라.**

| | 맡는 것 | 맡지 않는 것 |
|---|---|---|
| 구글 시트 | 우리 학교 행사 확정일 20~30줄 · 부장들이 공동 편집 | 계산, 절차 지식, 화면 |
| 이 앱 | 역산 · 기안 방법 안내 · 강조 · 필터 · 체크 · 인쇄 | 데이터 보관, 공유 |

**시트 형식.** 첫 줄은 머리글, 이후 한 행이 업무 하나다. 열은 아래 두 개만 필수다.

```
업무,확정일
가을 운동회,2026. 9. 11.
졸업식,2027. 1. 9.
```

- 첫 칸은 **업무 이름을 한글로** 적는다. `sports-day` 같은 영문 id 를 부장이 손으로 치게 하면
  안 된다. 오타 하나로 그 줄이 통째로 무시된다.
- 앱은 **업무 이름과 영문 id 를 모두** 받아들인다. 이름은 띄어쓰기를 무시하고 견준다
  (「가을 운동회」와 「가을운동회」는 같은 것으로 본다).
- **업무 이름이 미리 채워진 시트 서식을 함께 제공한다.** 부장은 날짜 칸만 채운다.
  이름을 손으로 치지 않으니 오타가 날 자리가 없다.
- **업무 제목을 바꾸면 기존 시트가 깨진다.** 제목을 고칠 때는 옛 이름도 계속 찾을 수 있게
  해 두거나, 학교에 서식을 다시 배포해야 한다.
- 시트에 없는 업무는 시드의 기본 앵커를 그대로 쓴다. **시트는 덮어쓰기지 대체가 아니다.**
- 시트에 있는데 앱에 없는 이름은 조용히 무시하고, 화면 어딘가에 개수만 알린다.
- 날짜 칸이 비었거나 형식이 틀리면 그 줄만 건너뛰고 나머지는 정상 처리한다. **한 줄 오타로
  전체가 죽으면 안 된다.**

**읽는 방법.** 시트 「파일 → 공유 → 웹에 게시 → CSV」로 나온 주소를 `fetch`로 읽는다.
구글 로그인, API 키, 앱스스크립트를 쓰지 마라. 읽기 전용이며 앱은 시트에 절대 쓰지 않는다.

- 주소는 URL 해시(`#s=...`)로도 받을 수 있게 한다. 부장이 링크 한 번만 뿌리면 나머지 교직원은
  설정할 것이 없어야 한다. 해시로 들어온 주소는 localStorage에도 저장해 다음부터는 그냥 열린다.
- **브라우저 캐시를 우회하라.** 요청에 `cache: "no-store"`를 주거나 주소 끝에 타임스탬프를 붙인다.
  부장이 시트를 고쳤는데 옛날 날짜가 보이면 이 앱은 신뢰를 잃는다.
- **우선순위: 시트(학교 확정일) > 사용자가 앱에서 고친 값 > 시드(임시 배치).**
  부장이 학교 전체 사정을 알고 정한 날짜이므로 시트가 가장 앞선다. 개인이 고쳐 둔 값 때문에
  학교 확정일을 못 보게 되면 이 앱의 목적 자체가 무너진다.
  다만 사용자가 고쳐 둔 값을 지우지는 마라. 화면에서 밀려났다는 것을 알리고, 시트에서 그 업무가
  빠지면 자동으로 다시 쓰이게 한다. 조용히 없애면 사용자는 자기 수정이 사라진 줄 안다.
- **링크로 들어온 주소가 저장된 주소를 이긴다.** 학교가 시트를 새로 만들면 부장이 새 링크를
  뿌린다. 저장된 옛 주소가 이기면 새 링크를 눌러도 옛날 시트를 계속 보게 된다.
- 시트를 못 읽어도 앱은 죽지 않는다. 시드 기본값으로 정상 동작하되 화면에
  `학교 일정을 불러오지 못했습니다. 기본 일정으로 표시 중입니다.`라고 알린다.
- 마지막으로 성공한 시트 내용을 localStorage에 캐시해 둔다. 오프라인이거나 시트가 잠시 안 열릴 때
  그 값을 쓰고, `○월 ○일에 불러온 일정입니다`라고 표시한다.

## 4. 역산 계산 규칙 (이 앱의 핵심)

`resolveDate(task, subtask)` 함수를 만들어라.

1. `anchor.mode === "date"` → 기준일 = `anchor.date`
2. `anchor.mode === "week"` → 기준일 = 해당 월의 `week`번째 월요일.
   **학년도 경계 규칙:** 학년도는 당해 3월 1일부터 익년 2월 말일까지다.
   - `month`가 3~12 → 실제 연도는 `schoolYear`
   - `month`가 1~2  → 실제 연도는 `schoolYear + 1`
   - 예) `schoolYear: 2026`, `{ mode: "week", month: 1, week: 2 }` → **2027년** 1월 둘째 월요일
   - `week`번째 월요일이 그 달에 없으면(예: 월요일이 4번뿐인 달의 5주차) 그 달의 마지막 월요일로 처리한다.
3. 결과일 = 기준일 + `offsetDays`
4. **주말 보정** — 기본값 켜짐, 설정에서 끌 수 있다. 끄면 계산식 그대로(앵커 + `offsetDays`) 표시한다.

   | `offsetDays` | 의미 | 결과일이 토·일일 때 |
   |---|---|---|
   | `< 0` | 선행 준비 업무 | **직전 금요일로 당긴다.** 토 → −1일, 일 → −2일 |
   | `=== 0` | 앵커 당일 | **보정하지 않는다.** 아래 이유 참조 |
   | `> 0` | 후행 정산·보고 업무 | **다음 월요일로 미룬다.** 토 → +2일, 일 → +1일 |

   - **`offsetDays === 0`을 보정하지 않는 이유:** 앵커는 사용자가 직접 입력한 행사일이다. 앱이 임의로
     옮기면 사용자가 방금 넣은 날짜가 화면에서 다른 날로 바뀐다. 행사일이 토·일이면 그대로 표시한다.
   - 선행은 당기고 후행은 미루는 이유: 준비는 늦으면 안 되고, 정산은 빨라질 수 없다.
   - 보정은 **한 번만** 적용한다. 위 규칙의 결과가 다시 주말이 되는 경우는 없다.
   - **공휴일·재량휴업일은 보정하지 않는다.** 매년 바뀌고 학교마다 달라, 보정하면 오히려 틀린 날짜를 만든다.
   - 보정으로 날짜가 옮겨진 항목은 하위 업무 줄에 `주말 보정` 표시를 작게 붙인다. 교사가 "왜 이 날짜지?"
     하고 되묻지 않게 하는 것이 목적이다. 표시만 추가하는 것이며 별도 화면을 만들지 마라.
5. 계산 결과는 캐시하지 말고 렌더링 시점에 매번 계산한다. 앵커를 바꾸면 즉시 전부 재배치되어야 한다.

**이 재계산이 이 앱의 유일한 핵심 차별점이다.** 앵커 날짜 입력칸을 바꾸는 순간, 화면의 모든
파생 업무 날짜와 D-day 표시가 애니메이션 없이도 즉각 갱신되어야 한다. 여기에 가장 공을 들여라.

## 5. 화면 구성

### 5-1. 상단 바
- 앱 이름, 학년도 선택, 「예시 데이터로 둘러보기」 버튼, 「내 데이터 전체 삭제」 버튼
- 배지: `입력한 내용은 이 브라우저에만 저장되며, 외부로 전송되지 않습니다 (네트워크 요청 0건)`
  — 이 문구는 항상 보이는 위치에 둔다. 심사 기준 항목이다.

### 5-2. 필터 줄
- 분류 4종 토글 (법정·필수는 끌 수 없게 잠금 처리)
- 부서 드롭다운, 학년 드롭다운, 월 선택
- **학년 필터 규칙:** 학년 N을 고르면 `grades`에 N이 든 업무와 **`grades`가 비어 있는 전 학년 업무를
  함께** 보여준다. 비어 있는 것을 "해당 없음"으로 처리하면 법정 업무가 통째로 사라진다.
- 필터 상태는 localStorage에 저장되어 새로고침해도 유지된다

### 5-3. 메인 — 월별 타임라인
- 3월부터 다음 해 2월까지 세로 스크롤. 월 헤더 고정(sticky)
- 각 업무는 카드로 표시. 카드 안에:
  - 제목, 분류 뱃지, 부서, 앵커 날짜(인라인 편집 가능)
  - 근거(`basis`)를 작은 회색 글씨로. 없으면 표시 안 함
  - 하위 업무(subtask) 목록: `D-30 · 5. 8.(금) · 물품 구입 품의` 형태 + 체크박스
  - 각 하위 업무를 펼치면 기안 제목 예시(복사 버튼), 붙임 서류, 자주 틀리는 점 표시
- **오늘 기준 D-7 이내이면서 미완료인 항목은 눈에 띄게 강조**한다 (이 앱을 쓰는 이유다)

### 5-4. 하단 고정 문구
`본 일정은 참고용 표준안이며, 최종 시행일은 소속 학교·교육청 지침을 따릅니다.`

### 5-5. 온보딩
첫 실행 시 3단계 안내 오버레이. ①행사일 입력 → ②역산 확인 → ③체크. 다시 보지 않기 옵션.

## 6. 초기 데이터

`src/data/tasks.seed.ts`에 초등학교 공통 업무 시드 데이터를 넣는다.
분류별로 최소 이 정도 규모:
- 법정·필수 공통: 8건 이상 (정보공시, 학교평가, 각종 의무연수, 학교운영위원회 등)
- 통상 학사·연례 행사: 10건 이상 (학부모총회, 체육대회, 현장체험학습, 학예회, 졸업식 등)
- 공모·목적 사업: 3건 (기본 비활성)
- 학교 자율·특색: 빈 상태 + 추가 버튼

**offsetDays 는 규정이 아니라 제안값이다.** 어느 지침에서 가져온 숫자가 아니라 일반적인 추론으로
정한 값이다. 학교마다 달라 정답이 없다. 화면에서 `D-30` 이 규정처럼 보이면 전입교사는 그대로 믿는다.
- 업무 카드에 이 숫자가 학교 사정에 따라 조정할 수 있는 값임을 알린다.
- 현직 교사가 검토해 고친 항목은 시드에 검토 표시를 남겨 구분한다.
- 1차에서는 시트로 이 값을 바꿀 수 없다. 시트가 바꾸는 것은 행사일(앵커)뿐이다.
  준비 기간까지 학교별로 조정하는 것은 2차 과제 후보다.

**중요:** 법정 업무의 구체적 시행 시기는 네가 추측해서 확정하지 마라.
불확실한 항목은 `basis`를 빈 값으로 두고 `// TODO: 근거 확인 필요` 주석을 달아라.
내가 실제 지침을 보고 채우겠다. 잘못된 날짜가 들어가면 이 앱은 오히려 위험해진다.

## 7. 완료 기준 (전부 통과해야 끝)

- [ ] `dist-single/index.html` 하나만 **실제 학교 노트북**으로 복사해 더블클릭하면 오프라인으로
      완전 동작한다 (체크 저장 → 브라우저 완전히 닫기 → 다시 열기 → 체크가 남아 있는지까지 확인)
- [ ] 앵커 날짜를 바꾸면 파생 업무 날짜가 즉시 전부 재계산된다
- [ ] **역산 경계값 테스트가 전부 통과한다.** 아래 경우를 단위 테스트로 고정해 둔다.
      날짜 계산이 틀리면 이 앱은 안 쓰느니만 못하다.
      - 학년도 경계: `schoolYear 2026` + `{month:1, week:2}` → **2027년** 1월 둘째 월요일
      - 없는 주차: 월요일이 4번뿐인 달의 `week:5` → 그 달 마지막 월요일
      - 주말 보정 선행: 결과일이 토 → 금(−1), 일 → 금(−2)
      - 주말 보정 후행: 결과일이 토 → 월(+2), 일 → 월(+1)
      - 앵커 당일(`offsetDays: 0`)은 토·일이어도 **움직이지 않는다**
      - 보정 끔: 계산식 그대로 나온다
      - 월 경계·연 경계를 넘는 오프셋 (예: 3월 5일 앵커에 `-30`)
      - 윤년 2월 (2028년 2월 29일)
- [ ] 새로고침 후에도 체크 상태와 필터(부서·학년·분류·월)가 유지된다
- [ ] 시드 데이터에 업무를 1건 추가해도, 기존 사용자의 체크와 앵커 수정이 그대로 남는다
- [ ] 시트에서 날짜를 고치고 새로고침하면 앱 화면에 즉시 반영된다 (브라우저 캐시에 걸리지 않는다)
- [ ] 시트 주소가 담긴 링크를 처음 받은 사람이 **아무 설정 없이** 학교 일정을 본다
- [ ] 시트를 못 읽는 상황(주소 오류·네트워크 차단)에서도 앱이 기본 일정으로 정상 동작한다
- [ ] 시트 한 줄에 오타가 있어도 나머지 줄은 정상 처리된다
- [ ] 개발자 도구 Network 탭에 **시트 읽기 외의 외부 요청이 0건**이다
- [ ] 「내 데이터 전체 삭제」 후 초기 상태로 완전히 돌아간다
- [ ] 코드 전체에 학생·교직원 성명을 담는 필드가 없다
- [ ] 1024px 이하 화면에서도 레이아웃이 깨지지 않는다 (학교 노트북 대응)
- [ ] `npm run build` 산출물을 GitHub Pages에 올렸을 때 경로 문제 없이 열린다

## 8. 작업 순서

1. 프로젝트 초기화 + 두 종류 빌드 설정 + GitHub Pages 자동 배포 → **여기서 멈추고 확인 요청**
2. 데이터 모델 + localStorage 래퍼(`v` 검증 포함) + 역산 계산 함수 + 단위 테스트
3. 시드 데이터 작성 (불확실한 항목은 TODO 주석)
4. **구글 시트 읽기** — CSV 파싱, URL 해시로 주소 전달, 캐시 우회, 실패 시 기본값 동작 + 단위 테스트
5. 월별 타임라인 + 업무 카드 UI
6. 필터 · 체크 · 앵커 인라인 편집
7. 온보딩, 예시 데이터, 전체 삭제, 안내 문구
8. 반응형 점검 + 완료 기준 자체 검증 → 결과를 표로 보고 (여기까지 10. 20.)
9. **팀 상호 테스트 반영** — 초등학교팀 2인 이상이 실제로 써 본 뒤 나온 지적 사항을 반영한다.
   기능 추가 요청이 나오면 구현하지 말고 2차 과제 후보로 목록만 남겨라.
10. **제출물 준비** (10. 21. ~ 10. 30.)
   - 화면 캡처 3장 (앵커 수정 전후 비교 1장 포함. 이 앱의 차별점이 이 한 장에 담긴다)
   - 사용 안내 1쪽 PDF — 문제 · 해결 · 화면 · 효과 순서
   - GitHub Pages 배포 링크 + `dist-single/index.html` 파일

## 9. 하지 말아야 할 것

- 기능을 늘리지 마라. 목록에 없는 기능은 제안만 하고 구현하지 마라.
- **다음 두 가지는 2차 과제 범위다. 1차에서 구현하지 마라.**
  - 캘린더 `.ics` 파일 내보내기
  - 1쪽 인수인계서 내보내기
  (기획 PPT 초안에 잘못 들어갔던 항목이다. 1차는 인쇄용 CSS까지만 한다.)
- 다크모드, 애니메이션 라이브러리, 아이콘 패키지 추가 금지 (번들만 커진다)
- `any` 타입 남발 금지
- 화면에 영어 UI 문구를 남기지 마라
- 내가 확인하지 않은 단계로 넘어가지 마라

---
